<UserControl x:Class="Juego_PA.Views.NIvel2View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Juego_PA.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="GhostWhite">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="Losentimos, aun no se ha programado este nivel" 
                       HorizontalAlignment="Center"
                       FontSize="25"/>
            <Button Height="50"
                    Margin="15"
                    Content="Regresar"
                    Command="{Binding CambiarVistaCommand}"
                    IsDefault="False"
                    CommandParameter="{x:Static local:Vista.SeleccionarNivel}" />
        </StackPanel>
    </Grid>
</UserControl>
